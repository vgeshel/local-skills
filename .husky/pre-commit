#!/usr/bin/env sh
pnpm run typecheck
pnpm exec tsx scripts/check-lint-exceptions.ts
pnpm run lint

# Verify vitest coverage thresholds remain at 100%
# Prevents lowering of code coverage requirements
pnpm exec tsx scripts/check-coverage-thresholds.ts

pnpm exec lint-staged
